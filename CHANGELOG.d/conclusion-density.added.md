Conclusion-density primitives: `trace` and `search` accept `count_only=true` and return only the counts (`{root, direction, total, by_depth, by_risk}` / `{query, total, by_kind}`) — computed from the exact same store paths as the row-shaped call, composing with every existing filter, so an agent asking "how many callers?" pays tens of tokens instead of N rows. New read-only lite-tier tool `assert_graph` evaluates structural invariants entirely server-side against the edge graph (`no_callers_outside`, `max_callers`, `no_calls_to`, `exists`) and returns `{pass, checked}` when passing, plus up to 10 `{id, file_path}` violations when failing; unknown kinds rich-error with the full catalog
