Forward-port the v0.4 → v1.0 migration guide through v0.97, fix stale schema-v32 walkthrough text, add an external-review packet for the #1390 signoff gate, harden migration rehearsal so the HTTP probe uses loopback-only HTTP without stdio and fails after a bounded readiness wait, and align local service/dashboard docs with the default-deny HTTP bind rule.
