**Tier 1 test-coverage hardening for v1.1.0-rc.2 / v1.2.0-rc.2.** Three additive gates around the v1.2 surface so regressions surface in CI instead of dogfood. (a) Cross-language `pincher report` markdown fidelity snapshots against `testdata/corpus/{python-app, go-project, node-monorepo}` — `cmd/pinch/report_corpus_fidelity_test.go`, golden files at `testdata/corpus/<name>.report.md`, regenerate with `go test ./cmd/pinch -run TestReportCorpus -update-report-corpus`. (b) `pincher_report.v1` JSON contract test pinning the top-level fields and `next_pincher_calls[*].args` structured shape so removal/rename surfaces immediately — `cmd/pinch/report_v1_contract_test.go`. (c) Race detector workflow promoted from cron-only to fire on every `v*` tag push, so every RC and stable tag now pays the ~20–25 min race-instrumented test bill before its artifacts are trusted — `.github/workflows/race-detector.yml`.
