Fix `pincher doctor --help` so the `--project` flag prints a useful `NAME` placeholder instead of leaking Go's backtick placeholder parsing, and document the doctor DB-size triage fields used during v0.99 hardening.
