**ADR-0008 accepted — Rust shipped at AST tier ([#1960](https://github.com/kwad77/pincher/issues/1960)).** Marked Accepted: the real-tree-sitter-via-WASM engine is proven in production with Rust extracting at confidence 1.0, and a "Realized outcomes & lessons" section now records what shipping it taught (the binding WASM-memory-leak + free-lifecycle fix and its no-leak/fuzz gates; matching the regex tier's QN conventions exactly to avoid symbol-ID churn; the per-file `HasError()`→regex dispatcher shape; and the CI subprocess-coverage-fold gotcha when wazero links into `cmd/pinch`) — the reusable recipe for the Java/TS/C# rollouts, also stored in pincher's own `adr` knowledge store.
