**Dead-code verdicts are now capped where the static graph is dispatch-blind.** `audit_unused` had labeled all nine live tree-sitter `extract` methods "confidence: high" with evidence `deep_trace_confirms_unreachable` — they are reached via the extractor registry's interface dispatch, which produces no CALLS edges, so an autonomous agent acting on the verdict would have deleted live code. Now: a Method whose case-folded name matches a project interface method, or any candidate extracted below AST-tier confidence (<0.95, regex-tier languages under-resolve cross-file calls), is capped at `medium` with per-candidate evidence (`interface_dispatch_possible` / `extraction_confidence_below_ast_tier_calls_under_resolved`) plus a `_meta.warnings_v2` `dispatch_blind` advisory. `dead_code` stamps the same evidence per row and the same advisory. Plain uncalled Functions in AST-tier languages keep the high-confidence path.
