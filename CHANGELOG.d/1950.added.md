`fetch` and `rebuild_fts` now emit MCP `notifications/progress` when the client supplies a progressToken (#1950, completing #1080): `fetch` reports bytes downloaded with a determinate total when `Content-Length` is present (indeterminate counter otherwise), and `rebuild_fts` reports six rebuild stages (drops, schema recreate, three per-corpus backfills, row count)
