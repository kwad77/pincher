**SPDX-License-Identifier headers on every `.go` file.** All 747 Go source files now carry `// SPDX-License-Identifier: MIT` at the top (after any leading `//go:build` constraint). Standard machine-readable license attribution that GitHub's dependency-graph, FOSSA, REUSE, and SBOM generators recognize. Helper script lives at `scripts/add-spdx-headers.py` and is idempotent (a second run is a no-op).
