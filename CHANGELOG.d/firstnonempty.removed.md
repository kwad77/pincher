Removed unused `firstNonEmpty` helper from `internal/server/investigate_failure.go`. It was the original implementation behind a `search` next-step builder; #1570 rewrote that path to do explicit `len(frameNames) > 0` branching for the "only file paths parsed" case, leaving the helper orphaned. Surfaced by an `audit_unused language=Go` dogfood probe (one of two flagged candidates this session — the other, `platformProcessExecutablePath`, was a false positive from missing var-binding edges and is tracked separately as #1877).
