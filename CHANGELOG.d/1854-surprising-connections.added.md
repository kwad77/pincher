`architecture` now surfaces `surprising_connections` — the rarest cross-package CALLS pairs ([#1854](https://github.com/kwad77/pincher/issues/1854)). Hotspots show the most-connected symbols; this shows the opposite signal: package pairs joined by just one or two calls, the fragile or hidden coupling points worth a reviewer's eye. Up to 10 directed pairs ranked rarest-first, each with an example edge, computed deterministically from pincher's AST-derived CALLS edges.
