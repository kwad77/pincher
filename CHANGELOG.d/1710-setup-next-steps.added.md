`pincher setup` now prints a `Next steps` block to the surviving terminal output ([#1710](https://github.com/kwad77/pincher/issues/1710) v0.92). The wizard's done screen showed next-steps guidance, but it was wiped when the wizard exited — so the load-bearing instruction (restart your editor / MCP client so it picks up the pincher server) never survived. The cooked-terminal summary now re-emits it, plus the `pincher index` and `pincher doctor` follow-ups, adapting to whether the user already chose to index.
